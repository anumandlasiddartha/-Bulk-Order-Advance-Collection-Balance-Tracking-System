"""
Cakes and Crunches — Controllers Package

Flask Blueprints that define REST API routes.
Controllers are thin — they validate input via schemas,
delegate to services, and return serialized responses.
"""
